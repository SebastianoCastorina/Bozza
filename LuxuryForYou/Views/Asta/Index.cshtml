@model IEnumerable<LuxuryForYou.Models.Asta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Autovettura.NomeModello)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Autovettura.NomeModello)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idAsta }) |
            @Html.ActionLink("Details", "Details", new { id=item.idAsta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idAsta })
        </td>
    </tr>
}

</table>
